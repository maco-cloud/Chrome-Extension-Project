# Backend API (no provider keys in the extension)
VITE_API_BASE_URL=https://api.example.com/v1

# Auth (Supabase — wire when backend is live)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe publishable key only (secret keys stay on server)
VITE_STRIPE_PUBLISHABLE_KEY=

# Checkout URLs from your backend (Stripe Checkout Session)
VITE_STRIPE_CHECKOUT_MONTHLY_URL=
VITE_STRIPE_CHECKOUT_YEARLY_URL=
